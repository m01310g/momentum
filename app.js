// boolean 사용
// null은 존재하지 않음, 자연적으로 발생하지 않고 선언을 해줘야 함(값이 존재하지 않음을 의도적으로 표현)
// undefined는 변수가 메모리에는 있는데 값이 존재하지 않음(비어 있음)
// false는 '거짓'이라는 값이 존재
const amIFat = null;
let something;  // something -> undefined

console.log(something, amIFat);